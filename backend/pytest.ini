[pytest]
; Redirect pytest's temp/cache directories away from the OS temp folder and
; the repo's default .pytest_cache -- both of which can end up with broken
; permissions on some Windows setups. These are safe to delete anytime.
addopts = --basetemp=.pytest_tmp
cache_dir = .pytest_cache_local
